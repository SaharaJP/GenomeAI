# 🧬 GenomeAI — No-Code Платформа Генетического Анализа (MVP)

## 📘 Описание проекта

**GenomeAI** — это **no-code платформа** для воспроизводимого и безопасного запуска геномных анализов.  
Она позволяет биологам и врачам **выполнять сложные пайплайны NGS-анализа без программирования и DevOps-рутины**.

Система автоматизирует путь **«от FASTQ до отчёта»** — от загрузки данных до готовых VCF-файлов и отчётов качества (MultiQC, PDF).  
Проект реализует идею *reproducible, auditable, and scalable genomic analysis for everyone*.

---

## 🚀 Ключевые преимущества

- **No-code интерфейс:** запуск анализа в несколько кликов.  
- **Воспроизводимость:** автоматическая фиксация всех версий workflow, Docker-образов и reference-сетів.  
- **Экономия:** кэширование Nextflow (`-resume`) переиспользует до **80%** результатов.  
- **Безопасность:** шифрование, RBAC, аудит, on-premise-вариант.  
- **Готовность к масштабированию:** от single-node Docker до SLURM/AWS Batch (Phase 2).  
- **Поддержка стандартов nf-core / GA4GH (WES, DRS)** — в дорожной карте.  

---

## 🏗 Архитектура

```
Пользователь → Web-UI → API → Runner → Nextflow + Docker
                          ↓
                    PostgreSQL / MinIO / Redis
```

### Основные компоненты

| Компонент | Описание |
|------------|-----------|
| **Frontend (Next.js/React)** | Веб-интерфейс: загрузка данных, редактор workflow, мониторинг запусков, отчёты. |
| **Backend API (FastAPI)** | Управление проектами, пользователями, данными, запусками и артефактами. |
| **Runner** | Оркестратор Nextflow-джоб, стрим логов и контроль выполнения. |
| **PostgreSQL** | Хранение метаданных (пользователи, проекты, запуски, артефакты). |
| **MinIO (S3)** | Хранилище файлов: FASTQ, BAM/VCF, отчёты. |
| **Redis** | Очередь задач и кэш для Runner. |
| **Nextflow + Docker** | Исполнитель пайплайнов (по умолчанию профайл `local-docker`). |

---

## ⚙️ Минимальные требования (MVP)

| Ресурс | Значение |
|--------|-----------|
| CPU | ≥ 32 vCPU |
| RAM | ≥ 128 GB |
| NVMe | ≥ 2 TB (рабочий каталог `work/`) |
| HDD/S3 | ≥ 10 TB (артефакты и отчёты) |
| ОС | Ubuntu 20.04+ / Rocky 9+ |
| Docker / Compose | последняя стабильная версия |

---

## 🔩 Установка (single-node MVP)

1. Клонировать репозиторий и скопировать `.env.example → .env`.  
2. Настроить параметры:
   ```bash
   DATABASE_URL=postgresql://user:pass@postgres/genomeai
   S3_ENDPOINT=http://minio:9000
   S3_ACCESS_KEY=...
   S3_SECRET_KEY=...
   OIDC_ENABLED=false
   ```
3. Запустить:
   ```bash
   docker-compose up -d
   ```
4. Проверить:
   ```bash
   curl https://localhost/healthz
   ```
5. Открыть интерфейс: [https://localhost](https://localhost)

---

## 🧭 Логика работы (workflow end-to-end)

1. **Администратор** создаёт проект, добавляет участников и Reference Set (например, `GRCh38`).  
2. **Пользователь** загружает FASTQ или указывает S3-URI.  
3. Формирует *Sample Sheet* (R1/R2, ID, тип секвенирования).  
4. Выбирает пайплайн (`nf-core/dna-seq`) и референс.  
5. Запускает анализ (Compute Profile = `local-docker`).  
6. **Runner** вызывает `nextflow run`, управляет шагами, собирает артефакты.  
7. По завершении система:
   - сохраняет BAM/VCF/лог/отчёты в S3;  
   - формирует **JSON манифест** версий и checksum;  
   - рендерит **HTML → PDF отчёт** (MultiQC + QC метрики).  
8. Пользователь просматривает DAG, статус, загружает отчёт и артефакты.  
9. Возможен повторный запуск (**Re-run**, **Clone**) с теми же версиями.

---

## 📊 Содержание отчёта

Отчёт включает:
- Контекст запуска (Run ID, проект, дата, пользователь).  
- Workflow, git-ревизию, версии инструментов и контейнеров.  
- Использованный Reference Set + контрольные суммы.  
- QC-метрики: глубина, покрытие, Ti/Tv, дубликаты, контаминация, и т.д.  
- Ссылки на артефакты (VCF, BAM, MultiQC).  
- Контрольные суммы файлов и JSON манифест воспроизводимости.  

**Что можно узнать:**  
✔ Проверить корректность и качество данных;  
✔ Повторить анализ 1-в-1 на другой машине;  
✔ Подготовить отчёт для ревью, аудита или клиники;  
✔ Локализовать ошибку при сбое пайплайна.

---

## 🔐 Безопасность

- Шифрование данных (TLS ≥ 1.2, AES-256 at rest).  
- RBAC (Admin / Editor / Viewer).  
- Аутентификация через OIDC (Keycloak) или локальные аккаунты.  
- Неизменяемый аудит действий (хранение ≥ 12 мес).  
- Маскирование PHI/PII в логах и отчётах.  
- Ежедневные бэкапы БД и версионность S3-объектов.

---

## 🧪 Нефункциональные показатели (SLO)

| Метрика | Цель |
|----------|------|
| Время старта задачи | ≤ 60 сек |
| Полный прогон chr20 | ≤ 45 мин |
| Экономия при `-resume` | ≥ 80 % тасков |
| UI p95 latency | ≤ 2.0 сек |
| Доступность | ≥ 99.5 % |

---

## 🧱 Дорожная карта

| Этап | Цели |
|------|------|
| **MVP (текущий)** | Single-node Docker, nf-core/dna-seq, Reference Sets, отчёты, RBAC. |
| **Phase 2** | Поддержка SLURM / AWS Batch / Kubernetes; GA4GH WES / DRS; импорт пайплайнов из Dockstore; условные ветви, scatter/gather в редакторе; биллинг и мульти-тенантность. |

---

## 💰 Модель монетизации (для SaaS-варианта)

| Тариф | vCPU·ч | S3 | egress | Цена ₽/мес | SLA |
|--------|--------|----|---------|-------------|-----|
| **Минимальный** | 500 | 1.5 ТБ | 150 ГБ | 210 000 | 99 % |
| **Базовый** | 4 000 | 7 ТБ | 750 ГБ | 670 000 | 99.3 % |
| **Про** | 20 000 | 20 ТБ | 3 ТБ | 2 200 000 | 99.5 % |
| **On-premise** | – | – | – | По запросу | Custom |

---

## 🧩 API-эндпоинты (основные)

| Метод | Путь | Назначение |
|--------|------|------------|
| `GET /healthz` | Проверка статуса системы |
| `POST /auth/login` | Аутентификация |
| `GET /projects` | Список проектов |
| `POST /datasets/upload` | Загрузка FASTQ |
| `POST /runs` | Создание запуска |
| `GET /runs/{id}` | Детали и статус |
| `GET /reports/{id}` | Получение отчёта |

---

## 🧑‍💼 Роли и сценарии

| Роль | Действия |
|------|-----------|
| **Admin** | Развёртывание, управление пользователями, Reference Sets, политика хранения, метрики. |
| **Editor** | Загрузка данных, настройка workflow, запуск анализов, просмотр отчётов. |
| **Viewer** | Просмотр результатов и отчётов, загрузка артефактов. |

---

## 📦 Репозиторий и структура

```
/frontend/        → Next.js UI
/backend/         → FastAPI + Runner
/compose/         → docker-compose.yml, .env.example
/docs/            → Архитектура, ТЗ, FR/NFR
/tests/           → Unit / E2E
```

---

## 🧰 Поддержка и обратная связь

- **Технические вопросы:** support@genomeai.io  
- **Баг-репорты / идеи:** GitHub Issues  
- **Коммерческие предложения:** business@genomeai.io  

---

## ⚖️ Лицензия

© 2025 GenomeAI Project. Все права защищены.  
Использование материалов возможно только с письменного разрешения правообладателя.
